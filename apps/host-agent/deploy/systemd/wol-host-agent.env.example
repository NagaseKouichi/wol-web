HOST_AGENT_TOKEN=replace-with-a-long-random-token
HOST_AGENT_PORT=8765

# Optional command overrides:
# HOST_AGENT_SHUTDOWN_CMD=systemctl poweroff
# HOST_AGENT_REBOOT_CMD=systemctl reboot
# HOST_AGENT_SLEEP_CMD=systemctl suspend
