HOST_AGENT_TOKEN=replace-with-a-long-random-token
HOST_AGENT_PORT=8765

# Unraid-friendly default commands:
HOST_AGENT_SHUTDOWN_CMD=powerdown
HOST_AGENT_REBOOT_CMD=reboot
HOST_AGENT_SLEEP_CMD=sh -c echo -n mem > /sys/power/state
